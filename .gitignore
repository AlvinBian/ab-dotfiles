.DS_Store
*.local
.env
.env.*
!.env.template
*.log
.build.log
.update.log

# 用戶設定（從 config.template.json 複製）
config.json

# Build outputs（.plugin 檔 + 展開的預覽資料夾，僅供本地查閱，不追蹤）
dist/

# 快取（AI 分類、ECC 來源、session 等，不追蹤）
.cache/


# Node
node_modules/
pnpm-lock.yaml
.claude/worktrees/
.claude/settings.local.json
